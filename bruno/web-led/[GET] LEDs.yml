info:
    name: "[GET] LEDs (handleApiLedsGet)"
    type: http
    seq: 1

http:
    method: GET
    url: "{{HOST_BASE_URL}}/api/leds"
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
