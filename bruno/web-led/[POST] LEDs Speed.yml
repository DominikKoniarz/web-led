info:
  name: "[POST] LEDs Speed (handleApiLedsSpeedPost)"
  type: http
  seq: 5

http:
  method: POST
  url: "{{HOST_BASE_URL}}/api/leds/speed"
  body:
    type: json
    data: |-
      {
        "speedPercent": 50
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
