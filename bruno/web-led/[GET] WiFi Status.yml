info:
    name: "[GET] WiFi Status (handleApiWifiStatusGet)"
    type: http
    seq: 7

http:
    method: GET
    url: "{{HOST_BASE_URL}}/api/wifi/status"
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
