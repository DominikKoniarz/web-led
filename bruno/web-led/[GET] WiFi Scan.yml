info:
    name: "[GET] WiFi Scan (handleApiWifiScanGet)"
    type: http
    seq: 10

http:
    method: GET
    url: "{{HOST_BASE_URL}}/api/wifi/scan"
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
