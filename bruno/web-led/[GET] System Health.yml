info:
    name: "[GET] System Health (handleHealth)"
    type: http
    seq: 11

http:
    method: GET
    url: "{{HOST_BASE_URL}}/api/system/health"
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
